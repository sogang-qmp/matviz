node_modules/
dist/
*.vsix
.vscode-test/
.DS_Store
.claude/settings.local.json
test/visual/diff/

# TypeScript incremental + ESLint cache — regenerated by build/lint runs.
*.tsbuildinfo
.eslintcache

# General logs
*.log

# Editor / IDE noise
*.swp
*~
.idea/

# Windows
Thumbs.db
ehthumbs.db
desktop.ini

# v0.20 — spglib WASM is a third-party redistributable from npm. The .vsix
# package gets a fresh copy from node_modules at build time (esbuild.mjs
# copies it into dist/ + media/). No need to track in git.
media/moyo_wasm_bg.wasm
